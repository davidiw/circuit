# Copy to .env (git-ignored). The server loads this file at startup. There is no access gate: the prototype holds no sensitive content.
PORT=8797
AI_PROVIDER=none
AI_MODEL=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
