# Copy to .env (git-ignored). The server loads this file at startup; Vite exposes the VITE_ variables to the development gate only.
PORT=8797
SESSION_SECRET=replace-with-openssl-rand-hex-32
GATE_USER=change-me
GATE_PASS=change-me
VITE_DEV_GATE_USER=change-me
VITE_DEV_GATE_PASS=change-me
AI_PROVIDER=none
AI_MODEL=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
